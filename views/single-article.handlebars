{{!--pass array of revisions for the article_id--}}
<article class="single-article">

	<div>

			{{article.title}}
			</br>
			<a href="/article-history"></a>

		<p class="article-content">{{article.content}}</p>

	</div>
	<div class="meta">
		</br>
			written by {{article.user.username}}
		</br>

		{{format_date article.created_at}}, 
		{{format_time article.created_at}}
		</br>

		{{article.comments.length}} {{word_format "comment" comments.length}}
		</br>

	</div>
</article>
{{#if loggedIn}}
{{!--{{>add-comments}} - TODO - Create add-comments partial--}} 
{{/if}}
{{>comments}}